<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:bpm="http://www.mulesoft.org/schema/mule/bpm"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
          http://www.mulesoft.org/schema/mule/bpm http://www.mulesoft.org/schema/mule/bpm/current/mule-bpm.xsd">
    
    <bpm:config name="engineConfig" defaultTenantId="com.alfame.esb">
		<bpm:default-data-source >
			<bpm:generic-data-source driverClassName="org.h2.Driver" 
									jdbcUrl="jdbc:h2:mem:flowable;DB_CLOSE_DELAY=-1" 
									username="sa" 
									password="sa" 
									type="h2" />
		</bpm:default-data-source>
		<bpm:definitions>
			<bpm:classpath-definition resourceClasspath="processes/TestProcess.bpmn20.xml" />
		</bpm:definitions>
		<bpm:default-async-executor />
		<bpm:additional-tenants>
			<bpm:tenant tenantId="com.alfame.esb.sub.tenant">
				<bpm:definitions>
					<bpm:stream-definition resourceName="OtherTestProcess.bpmn20.xml" 
										resourceStream="#[readUrl( 'classpath://processes/sub/tenant/OtherTestProcess.bpmn20.xml', 'application/octet-stream' )]" />
				</bpm:definitions>
			</bpm:tenant>
		</bpm:additional-tenants>
	</bpm:config>
    
    <flow name="bpmListenerSuccessTestFlow">
        <bpm:task-listener config-ref="engineConfig" endpointUrl="bpm://some.success.test.queue" />
        <logger level="INFO" message="#[payload]" />
        <logger level="INFO" message="#[attributes]" />
        <set-payload value="SUCCESS" />
    </flow>

    <flow name="bpmListenerErrorTestFlow">
        <bpm:task-listener config-ref="engineConfig" endpointUrl="bpm://some.error.test.queue" />
        <set-payload value="BEFORE_ERROR" />
		<raise-error type="ANY" description="SOME_TEST_ERROR" />
        <error-handler>
            <on-error-propagate>
                <set-payload value="#[error.description]" />
            </on-error-propagate>
        </error-handler>
    </flow>
    
	<flow name="bpmTaskListenerTestFlow">
		<bpm:task-listener config-ref="engineConfig" endpointUrl="bpm://test.task" />
		<logger level="INFO" message="#[payload]" />

		<set-payload value="true" />
	</flow>

	<flow name="bpmOtherTaskListenerTestFlow">
		<bpm:task-listener config-ref="engineConfig" endpointUrl="bpm://other.test.task" />
		<logger level="INFO" message="#[attributes.id]" />

		<set-payload value="true" />
	</flow>

    <flow name="bpmStartProcessTestFlow">
        <bpm:process-factory config-ref="engineConfig" processDefinitionKey="otherTestProcess" tenantId="com.alfame.esb.sub.tenant" />
        <set-payload value="TEST" />
    </flow>

</mule>
